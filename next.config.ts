import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The floating dev badge sits in the corner of every recording. Off.
  devIndicators: false,
  /* config options here */
};

export default nextConfig;
