# Cashfree — sandbox test credentials from https://merchant.cashfree.com (Developers → API Keys)
CASHFREE_ENV=SANDBOX
CASHFREE_CLIENT_ID=
CASHFREE_CLIENT_SECRET=

# OpenAI — required for the agent itself (not for `npm run demo:lifecycle`)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1
